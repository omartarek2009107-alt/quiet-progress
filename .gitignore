node_modules/
out/
npm-debug.log*
.DS_Store

# Separate projects that happen to live in this folder — not part of
# Quiet Progress and deliberately kept out of this repo.
NetMonitor/
OledGuard/
ScreenTime/
